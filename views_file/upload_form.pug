doctype html
html
  head
    meta(charset='utf-8')
  body
    //- enctype이 있어야 파일이 올라간다.
    form(action='/upload' method='post' enctype='multipart/form-data')
      input(type='file' name='userfile')
      input(type='submit')